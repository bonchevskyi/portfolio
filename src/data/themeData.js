import {
  greenThemeLight,
  greenThemeDark,
  bwThemeLight,
  bwThemeDark,
} from "../theme/theme";

// first pass dark then light!

export const themeData = {
  theme: [greenThemeDark, greenThemeLight],
};
